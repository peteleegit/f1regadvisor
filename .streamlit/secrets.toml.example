# Copy to secrets.toml and fill in values. secrets.toml is git-ignored.

[auth]
password = "your-password-here"

[anthropic]
api_key = "sk-ant-..."

[fiaruler]
# FIARulerPro retrieval API (Railway internal URL in production)
api_url = "http://fiarulerpr.railway.internal:8000"
api_key = "your-retrieve-api-key"
